export const recommendedIgnorePatterns = ['coverage/**'];

const config = {
  printWidth: 100,
  singleQuote: true,
  bracketSpacing: false,
  trailingComma: 'es5',
  arrowParens: 'avoid',
  sortPackageJson: false,
};

export default config;
